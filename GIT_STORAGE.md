# 📦 Git Storage System - Prince Baby App

## نظرة عامة

يعتمد هذا المشروع على **Git Storage** كنظام تخزين رئيسي لإدارة بيانات التطبيق والتحكم بالنسخ. تم تصميم النظام لضمان:

- ✅ **التتبع الكامل** لكل تغيير في البيانات عبر Git history
- ✅ **فصل البيانات عن الكود** لتمكين فرق المحتوى من العمل بشكل مستقل
- ✅ **سلامة البيانات** عبر validation scripts و pre-commit hooks
- ✅ **إدارة الملفات الكبيرة** عبر Git LFS
- ✅ **التكوين المركزي** لخصائص التطبيق عبر ملفات JSON مُتتبعة

---

## 🏗️ هيكل التخزين

```
prince_baby_app/
├── data_storage/                    # ← Git Storage (البيانات المُدارة)
│   ├── products/
│   │   └── products.json           # بيانات المنتجات
│   ├── blog/
│   │   └── blog_posts.json         # مقالات المدونة
│   ├── faq/
│   │   └── faq.json                # الأسئلة الشائعة
│   ├── offers/
│   │   └── offers.json             # العروض والخصومات
│   ├── partners/
│   │   └── partners.json           # الشركاء ودليل المقاسات
│   ├── tips/
│   │   └── tips.json               # نصائح العناية
│   ├── config/
│   │   └── app_config.json         # إعدادات التطبيق
│   └── CHANGELOG.json              # سجل تغييرات البيانات
│
├── assets/                          # ← Git LFS (الملفات الثنائية)
│   └── images/                     # الصور (تُدار عبر LFS)
│
├── lib/
│   ├── services/
│   │   ├── git_storage_service.dart # خدمة تحميل البيانات
│   │   └── data_loaders.dart       # مُحمّلات البيانات المتخصصة
│   └── data/                       # ← البيانات القديمة (سيتم إزالتها تدريجياً)
│
├── scripts/
│   ├── validate_data.dart          # سكريبت التحقق من صحة البيانات
│   └── hooks/
│       └── pre-commit              # Git hook للتحقق التلقائي
│
├── .gitattributes                   # إعدادات Git LFS
├── .gitignore                      # استثناءات Git
└── GIT_STORAGE.md                  # هذا الملف
```

---

## 📐 معمارية النظام

```
┌─────────────────────────────────────────────────┐
│                   Flutter App (UI)               │
│              الشاشات والعناصر المرئية              │
├─────────────────────────────────────────────────┤
│            Data Loaders (Dart)                   │
│     مُحمّلات تحويل JSON → كائنات Dart             │
│  ProductsLoader | BlogLoader | FaqLoader | ...   │
├─────────────────────────────────────────────────┤
│          GitStorageService (Core)                 │
│    خدمة مركزية: تحميل + تخزين مؤقت + إصدارات      │
├─────────────────────────────────────────────────┤
│            data_storage/ (JSON)                  │
│     ملفات بيانات مُهيكلة مع metadata              │
│        ← مُدارة ومُتتبعة عبر Git →                │
├─────────────────────────────────────────────────┤
│              Git Repository                      │
│   Version Control | History | Branching | LFS    │
└─────────────────────────────────────────────────┘
```

---

## 🔧 كيفية الاستخدام

### 1. تحميل البيانات في التطبيق

```dart
import 'package:prince_baby_app/services/data_loaders.dart';

// تحميل المنتجات
final productsData = await ProductsLoader.loadAll();
final products = productsData['products'];
final categories = productsData['categories'];

// تحميل المدونة
final blogData = await BlogLoader.loadAll();
final posts = blogData['posts'];

// التحقق من تفعيل ميزة
final isCartEnabled = await AppConfigLoader.isFeatureEnabled('enableCart');
```

### 2. تعديل البيانات

```bash
# 1. إنشاء فرع للتعديل
git checkout -b data/update-products

# 2. تعديل ملف البيانات
# عدّل data_storage/products/products.json

# 3. تحديث metadata
# غيّر version و lastUpdated في _metadata

# 4. التحقق من صحة البيانات
dart run scripts/validate_data.dart

# 5. عمل commit
git add data_storage/
git commit -m "data: تحديث أسعار المنتجات"

# 6. دمج مع الفرع الرئيسي
git checkout main
git merge data/update-products
```

### 3. إضافة منتج جديد

أضف عنصراً جديداً في مصفوفة `products` داخل `data_storage/products/products.json`:

```json
{
  "id": 9,
  "name": "المنتج الجديد",
  "category": "حفاضات",
  "size": "3",
  "description": "وصف المنتج",
  "features": ["ميزة 1", "ميزة 2"],
  "colorKey": "babyBlue",
  "rating": 4.5,
  "price": 5000,
  "badge": null,
  "image": "https://..."
}
```

**ملاحظة:** `colorKey` يجب أن يكون أحد القيم: `primaryPink`, `babyBlue`, `softGreen`, `coralRed`

---

## 🔐 إدارة التعارضات (Conflict Resolution)

### استراتيجية الفروع المنفصلة

```
main ─────────────────────────────── (الإنتاج)
  │
  ├── data/update-products ────────── (تحديث المنتجات)
  │
  ├── data/new-blog-posts ─────────── (مقالات جديدة)
  │
  └── feature/new-screen ──────────── (تطوير الكود)
```

### قواعد منع التعارضات

1. **فرع واحد لكل ملف بيانات**: لا تعدّل نفس ملف JSON في فرعين مختلفين
2. **أضف دائماً في نهاية المصفوفات**: لتقليل احتمال التعارض
3. **استخدم IDs فريدة**: لتسهيل الدمج
4. **حدّث metadata**: غيّر `version` و `lastUpdated` عند كل تعديل

### حل التعارضات

```bash
# عند حدوث تعارض في ملف JSON
git mergetool --tool=vscode

# إعادة التحقق بعد الحل
dart run scripts/validate_data.dart
```

---

## 📊 Git LFS - الملفات الكبيرة

### الملفات المُدارة بـ LFS

| النوع | الامتدادات | السبب |
|---|---|---|
| صور | `.png`, `.jpg`, `.webp`, `.svg` | ملفات ثنائية لا تستفيد من diff |
| خطوط | `.ttf`, `.otf`, `.woff` | ملفات ثنائية كبيرة |
| فيديو | `.mp4`, `.mp3` | حجم كبير جداً |
| بناء | `.apk`, `.aab`, `.ipa` | مخرجات بناء |

### تثبيت Git LFS

```bash
# 1. تثبيت Git LFS
git lfs install

# 2. التحقق من الإعدادات
git lfs track

# 3. دفع الملفات
git lfs push --all origin
```

---

## 🧪 التحقق والاختبار

### تشغيل سكريبت التحقق

```bash
dart run scripts/validate_data.dart
```

### ما يتحقق منه:

- ✅ وجود جميع ملفات البيانات المطلوبة
- ✅ صحة صيغة JSON
- ✅ وجود `_metadata` مع `version` و `lastUpdated`
- ✅ عدم تكرار IDs في المنتجات والمقالات
- ✅ وجود جميع الحقول المطلوبة
- ✅ اكتمال أقسام ملف الإعدادات

### تثبيت Git Hook التلقائي

```bash
# نسخ hook إلى مجلد Git
cp scripts/hooks/pre-commit .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```

---

## 📋 أفضل الممارسات

### رسائل Commit للبيانات

استخدم بادئة `data:` لتمييز تغييرات البيانات عن الكود:

```
data: إضافة منتج جديد - حفاضات مقاس 7
data: تحديث أسعار العروض
data: إضافة 3 مقالات جديدة للمدونة
config: تفعيل ميزة الإشعارات
config: تحديث إعدادات API
```

### إصدار البيانات (Data Versioning)

- استخدم [Semantic Versioning](https://semver.org/):
  - `MAJOR.MINOR.PATCH`
  - **MAJOR**: تغيير هيكلي (إضافة/حذف حقول)
  - **MINOR**: إضافة بيانات جديدة
  - **PATCH**: تصحيح بيانات موجودة

### النسخ الاحتياطي

```bash
# إنشاء tag لحالة البيانات
git tag -a data-v1.0.0 -m "النسخة الأولى من البيانات"

# العودة لنسخة سابقة
git checkout data-v1.0.0 -- data_storage/
```

---

## 🚀 خطوات التطوير المستقبلية

1. **مرحلة 2**: ربط مع API خارجي مع الاحتفاظ بـ Git Storage كمرجع
2. **مرحلة 3**: إضافة CI/CD pipeline للتحقق التلقائي
3. **مرحلة 4**: دعم التخزين المختلط (Git + Firebase/Supabase)
4. **مرحلة 5**: إضافة نظام مراجعة البيانات (Data Review)

---

## ❓ الأسئلة الشائعة

### لماذا Git Storage وليس قاعدة بيانات تقليدية؟

| Git Storage | قاعدة بيانات تقليدية |
|---|---|
| تتبع كامل للتغيرات مدمج | يحتاج audit log منفصل |
| مراجعة كود للبيانات (PR) | لا يوجد |
| rollback فوري | يحتاج نسخ احتياطية |
| يعمل offline | يحتاج اتصال |
| مجاني بالكامل | قد يكون مدفوعاً |

### متى لا يجب استخدام Git Storage؟

- بيانات المستخدمين الديناميكية (تسجيل، طلبات)
- بيانات تتغير بشكل متكرر جداً (> 100 تغيير/يوم)
- ملفات ثنائية كبيرة جداً (> 100MB بدون LFS)
- بيانات تحتاج queries معقدة

---

*آخر تحديث: 2026-02-14*
